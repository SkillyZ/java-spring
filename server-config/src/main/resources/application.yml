spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      server:
          git:
            uri: https://github.com/forezp/SpringcloudConfig/
            force-pull: true
            searchPaths: respo
            username:
            password:
server:
  port: 8888


